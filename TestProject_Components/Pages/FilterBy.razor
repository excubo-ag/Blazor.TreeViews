@page "/filter"
<SourceCodeLink Repository="Blazor.TreeViews" Page="@nameof(FilterBy)" />

Select what's filtered:
<TreeView Items="Items" GetParent="(_) => null" AllowSelection="true" @bind-SelectedItems="SelectedValues" />

@code {
    private List<string> SelectedValues = new List<string>
    {
        "3.1.1"
    };
    private List<string> Items = new List<string>
    {
        "1",
        "1.1",
        "1.2",
        "2",
        "3",
        "3.1",
        "3.1.1",
        "3.1.2"
    };
    private string GetParent(string item)
    {
        var last_period = item.LastIndexOf('.');
        if (last_period < 0)
        {
            return null; // no parent
        }
        else
        {
            return item.Substring(0, last_period);
        }
    }
}

Remaining is:
<TreeView Items="Items" GetParent="GetParent" FilterBy='(e) => e.Where(v => !SelectedValues.Contains(v))' />
